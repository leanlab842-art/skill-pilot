下記コマンドで入っているかの確認
brew -v
git --version
gh --version
dotnet --version
node -v
docker --version
psql --version
------------
① Command Line Tools
        ↓
② Homebrew
        ↓
③ GitHub CLI
        ↓
④ Cursor
        ↓
⑤ .NET SDK
        ↓
⑥ Node.js
        ↓
⑦ Docker Desktop
        ↓
⑧ PostgreSQL
        ↓
⑨ DBeaver
        ↓
⑩ Postman
        ↓
⑪ GitHubからClone
        ↓
⑫ Cursorで開く


------------------
①コマンドラインツールが入っていなかったのでイントール（１０分くらいかな）
終了後確認
git --version
git version 2.xx.x　と出ればOK
-----------------
②下記コマンド入力
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
マックのパスワードを聞かれるので入力後Enter
Press RETURNが表示されたらEnter

下記コマンドを入力後、Homebrew 4.6.0のような表示がされればOK
brew -v

私が出たエラー
brew -vを入力後、「zsh: command not found: brew」が表示された。
原因はHomebrewはインストール済み
Macが「brewコマンドの場所」をまだ知らないとのこと

下記実行後、正しく認識されていることを確認
echo >> ~/.zprofile
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"

-------------------
③Github　CLI
下記コマンド入力
brew install gh
確認
gh --version

Githubログイン
下記コマンド入力
gh auth login

選択肢は下記　Enterで進む
GitHub.com
HTTPS
Login with a web browser

ブラウザが開くので認証スタート
認証確認後は下記コマンドにて確認
gh auth status

---------------
④Cursor
