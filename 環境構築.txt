下記コマンドで入っているかの確認
brew -v
git --version
gh --version
dotnet --version
node -v
docker --version
psql --version
------------
① Command Line Tools
        ↓
② Homebrew
        ↓
③ GitHub CLI
        ↓
④ Cursor
        ↓
⑤ .NET SDK
        ↓
⑥ Node.js
        ↓
⑦ Docker Desktop
        ↓
⑧ PostgreSQL
        ↓
⑨ DBeaver
        ↓
⑩ Postman
        ↓
⑪ GitHubからClone
        ↓
⑫ Cursorで開く


------------------
①コマンドラインツールが入っていなかったのでイントール（１０分くらいかな）
終了後確認
git --version
git version 2.xx.x　と出ればOK
-----------------
②下記コマンド入力
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
マックのパスワードを聞かれるので入力後Enter
Press RETURNが表示されたらEnter

下記コマンドを入力後、Homebrew 4.6.0のような表示がされればOK
brew -v

私が出たエラー
brew -vを入力後、「zsh: command not found: brew」が表示された。
原因はHomebrewはインストール済み
Macが「brewコマンドの場所」をまだ知らないとのこと

下記実行後、正しく認識されていることを確認
echo >> ~/.zprofile
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"

-------------------
③Github　CLI
下記コマンド入力
brew install gh
確認
gh --version

Githubログイン
下記コマンド入力
gh auth login

選択肢は下記　Enterで進む
GitHub.com
HTTPS
Login with a web browser

ブラウザが開くので認証スタート
認証確認後は下記コマンドにて確認
gh auth status

---------------
④Cursor
cursorをダウンロード
セットアップ完了後cursor上で下記入力実行
Install 'cursor' command

ターミナルにて下記実行
cursor --version
---------------
⑤.NET SDK
下記実行
brew install --cask dotnet-sdk

実行後確認
dotnet --version
8.x または 9.x が表示されればOKです。
私は10.xxxが表示された。あとで確認。
---------------
⑥node.js
下記コマンド実行
brew install node

確認
node -v
確認
npm -v
-------------
⑦Docker Desktop
brew install --cask docker

その後

Applications

↓

Docker

を起動

初回起動は数分かかります。

確認

docker --version
------------------
⑧postgresSQL
インストール

brew install postgresql@17

起動

brew services start postgresql@17

確認

psql --version
-----------------
⑨DBeaver
GUIでDBを見るためのツールです。

brew install --cask dbeaver-community
------------------
⑩postman
API確認ツール

brew install --cask postman
------------------
⑪開発フォルダ
私はここをおすすめします。

mkdir ~/Develop

移動

cd ~/Develop
STEP12 GitHubからClone
git clone https://github.com/leanlab842-art/skill-pilot.git

確認

cd skill-pilot
ls

README.md

などが見えればOK。
--------------------
13 Cursorで開く
cursor .

これでプロジェクトが開きます。
---------------------
14 ディレクトリ作成
mkdir docs
mkdir backend
mkdir frontend
mkdir scripts

mkdir backend/src
mkdir backend/tests

mkdir frontend/src
mkdir frontend/public

mkdir .github
--------------------
15 ドキュメント作成
touch docs/requirements.md
touch docs/api.md
touch docs/db.md
touch docs/architecture.md
touch docs/screen-design.md
touch docs/meeting-notes.md
touch docs/environment-setup.md
-----------------------------
16 Git操作

変更確認

git status

追加

git add .

コミット

git commit -m "chore: initialize project structure"

Push

git push

